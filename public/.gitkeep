# Keeps the public/ directory present so the Dockerfile's `COPY public ./public` always has a
# source. Put static assets you want served at the site root here (the App Router serves
# app/favicon.ico without needing this dir).
